# Anton Udovychenko — Portfolio

Single-page portfolio (Automation QA Engineer), static HTML deployed to GitHub Pages.

## Deploy

Pages for a `username.github.io` repo publishes automatically from `main` — no extra setup needed.
Optionally, switch **Settings → Pages → Source** to `GitHub Actions` to use `.github/workflows/deploy.yml` instead.
